import { type LightbulbIcon as LucideProps, Rocket } from "lucide-react"

export const ForBetLogo = (props: LucideProps) => (
  // Using Rocket as a placeholder, replace with your actual SVG or component
  <Rocket {...props} />
)

// You can add other custom icons here
